'use client';
import { useParams } from 'next/navigation'
import React from 'react'

const ViewProduct = () => {

  const { id } = useParams();

  return (
    <div>

    </div>
  )
}

export default ViewProduct
